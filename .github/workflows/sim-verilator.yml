name: CoralNPU RTL Verilator Simulation
on:
  workflow_dispatch:
  pull_request:
  push:
    tags:
      - "*"
    branches:
      - main

jobs:
  build-verilator:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 30
    env:
      CI_BUILD_STAGE_NAME: build
      CI_RUNS_ON: ubuntu-24.04
      CI_OS_NAME: linux
      CI_VERILATOR_INSTALL_DIR: ${{ github.workspace }}/verilator_install
    steps:
      - name: clone verilator
        run: |
          git clone https://github.com/verilator/verilator
      - name: install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang git autoconf bc bison build-essential help2man curl device-tree-compiler flex libfl-dev fuse3 gawk mold python-is-python3 python3 python3-full python3-pip python3-pyelftools python3-setuptools python3-venv srecord unzip xxd zip openjdk-17-jre z3
      - name:
        run: |
          pushd ${{ github.workspace }}/verilator
          autoconf
          ./configure --prefix="${{ env.CI_VERILATOR_INSTALL_DIR }}"
          make -j $(nproc)
          make install
          popd
      - uses: actions/upload-artifact@v4
        with:
          name: verilator
          path: |
            ${{ env.CI_VERILATOR_INSTALL_DIR }}
  coralnpu-sim-verilator:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 120
    needs:
      - build-verilator
    env:
      CI_BUILD_STAGE_NAME: build
      CI_RUNS_ON: ubuntu-24.04
      CI_OS_NAME: linux
      CI_TMPDIR: /tmp
      CI_CORALNPU_MPACT_COMMIT: 61a6317aca4de62a4862181d24dc22a1795bda43
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
          ssh-key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
      - uses: bazel-contrib/setup-bazel@0.15.0
        with:
          # Avoid downloading Bazel every time.
          bazelisk-cache: true
          # Store build cache per workflow.
          disk-cache: ${{ github.workflow }}
          # Share repository cache between workflows.
          repository-cache: true
      - uses: actions/download-artifact@v8
        with:
          name: verilator
          path: ${{ env.CI_TMPDIR }}/verilator_install
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang git autoconf bc bison build-essential help2man curl device-tree-compiler flex libfl-dev fuse3 gawk mold python-is-python3 python3 python3-full python3-pip python3-pyelftools python3-setuptools python3-venv srecord unzip xxd zip openjdk-17-jre z3
      - name: Clone UVM-1.2
        run: |
          git clone https://github.com/chipsalliance/uvm-verilator.git -b uvm-1.2 ${{ env.CI_TMPDIR }}/uvm-verilator
          git -C ${{ env.CI_TMPDIR }}/uvm-verilator cherry-pick -n --strategy=recursive -X theirs 5a37baacfed0722b523b05decc9b94fe3e9efbe4
      - name: Clone CoralNPU-mpact
        run: |
          git clone https://github.com/google-coral/coralnpu-mpact.git ${{ env.CI_TMPDIR }}/coralnpu-mpact
          git -C ${{ env.CI_TMPDIR }}/coralnpu-mpact checkout ${{ env.CI_CORALNPU_MPACT_COMMIT }}
      - name: Generate RTL and run simulation
        run: |
          chmod +x -R ${{ env.CI_TMPDIR }}/verilator_install/bin
          export PATH=$PATH:${{ env.CI_TMPDIR }}/verilator_install/bin
          export UVM=${{ env.CI_TMPDIR }}/uvm-verilator
          export CORALNPU_MPACT=${{ env.CI_TMPDIR }}/coralnpu-mpact
          bazel build //hdl/chisel/src/coralnpu:rvv_core_mini_axi_cc_library_emit_verilog
          python3 utils/run_uvm_regression.py --simulator=verilator
      - uses: actions/upload-artifact@v7
        with:
          name: uvm_regression_results
          archive: false
          path: |
            ${{ github.workspace }}/uvm_regression_*.zip
  coralnpu-sim-verilator-bazel:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    timeout-minutes: 120
    env:
      CI_BUILD_STAGE_NAME: build
      CI_RUNS_ON: ubuntu-24.04
      CI_OS_NAME: linux
      CI_TMPDIR: /tmp
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 1
          ssh-key: ${{ secrets.ACTIONS_DEPLOY_KEY }}
      - uses: bazel-contrib/setup-bazel@0.15.0
        with:
          # Avoid downloading Bazel every time.
          bazelisk-cache: true
          # Store build cache per workflow.
          disk-cache: ${{ github.workflow }}
          # Share repository cache between workflows.
          repository-cache: true
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y clang git autoconf bc bison build-essential help2man curl device-tree-compiler flex libfl-dev fuse3 gawk mold python-is-python3 python3 python3-full python3-pip python3-pyelftools python3-setuptools python3-venv srecord unzip xxd zip openjdk-17-jre z3 libftdi-dev libftdi1-dev libelf-dev
      - name: Generate RTL and run simulation
        run: |
          bazel test $(bazel query 'filter("verilator_uvm_regression_*", tests(//...))')
      - uses: actions/upload-artifact@v7
        with:
          name: uvm_regression_results
          path: |
            ${{ github.workspace }}/bazel-out/k8-fastbuild/testlogs/**/verilator_uvm_regression_*/test.outputs/uvm_regression_batch/
